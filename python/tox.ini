[tox]
# Define which Python versions to test
envlist = py38, py39, py310

[testenv]
# Install dependencies required for testing
deps = pytest

# Run pytest automatically
commands = pytest tests/

# Ensure tox installs the package before running tests
usedevelop = true